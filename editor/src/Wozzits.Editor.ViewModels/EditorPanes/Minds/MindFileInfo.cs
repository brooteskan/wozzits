namespace Wozzits.Editor.ViewModels.EditorPanes.Minds;

// A project mind file the Minds menu lists: the display name (file stem) + its path.
public sealed record MindFileInfo(string Name, string Path);
